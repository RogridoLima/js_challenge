{
  "name": "box2",
  "version": "1.0.0",
  "description": "Well, seems like you're good with javascript enough to create a new project from scratch using NPM and adding mocha test lib on it. Now you need to init project again, install npm test dependencies (Mocha again) and make the code that actually will make tests work fine...",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^6.2.0"
  }
}
